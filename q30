#include <stdio.h>

int main()
{
    int X=5;
    int K=7;

    int T=(X^K);

    printf("Message Block X = %d\n",X);
    printf("MAC T = %d\n",T);

    int forged=X^(T);

    printf("Forged second block: %d\n",forged);

    return 0;
}
